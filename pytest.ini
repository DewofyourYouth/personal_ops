[pytest]
# Async tests are opt-in via @pytest.mark.asyncio (strict mode). Setting this
# explicitly keeps behavior identical across local and CI and silences the
# "asyncio_mode unset" warning.
asyncio_mode = strict
