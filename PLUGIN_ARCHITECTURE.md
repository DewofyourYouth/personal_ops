# Plugin Architecture

Moved. The design has graduated from idea-capture to a full spec:

→ **[PLUGIN_ARCHITECTURE_SPEC.md](PLUGIN_ARCHITECTURE_SPEC.md)**

It covers the module contract, the core registry/loader, enable/disable config, the
incremental (strangler) migration path, a worked new-module example, and how modules attach
their routes to the [dashboard API](DASHBOARD_API_SPEC.md).
